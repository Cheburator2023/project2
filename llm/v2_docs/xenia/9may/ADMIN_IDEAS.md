1. обязателна страница админки для управления v2 json и ui схемой и логикой для https://rjsf-team.github.io/react-jsonschema-form/docs/ 
 * нужно создать конструктор который будет генерировать json и ui схему для react json schema form. конструктор имеет панель с уже созданными кастомными компонентами/виджетами и стоковые обычные инпуты
 * нужно продумать как пользак будет свзяывать логику, например если поле А = 1, то показывать поле Б, зависимость между полями required, скрытие полей, валидацию, маски и тп и тд https://github.com/uinaf/react-json-logic через jsonlogic
 * нужно хранить историю изменения схемы в базе данных, давать возможность применять старые версии схемы, откатывать изменения до дефолтного состояния 
 * нужна безопасность - проверка на циклические зависимости и тп
 * в идеале схема и логика живет и изменяется с бека (не как в v1 - где всё на фронте
)